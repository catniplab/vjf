# direnv: resolve the shared catniplab.sty / math_preamble.tex without vendoring them.
# Run `direnv allow` once, or `export TEXINPUTS=...` manually before `make`.
export TEXINPUTS="$HOME/Dropbox/_projects/memmingCommons/latex/:$TEXINPUTS"
